" Auto-detect indentation based on filetype.
filetype plugin indent on

" Enable syntax highlighting.
syntax on

" Keep new line indentation the same as current line.
set autoindent

" Set indentation to 2 spaces.
set shiftwidth=2
set softtabstop=2
set expandtab
